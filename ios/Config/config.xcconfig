PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = Task Follow-up
BUNDLE_IDENTIFIER = taskfollowup.ocpgroup.ma

RUNTIME_URL = https:\/\/192.168.1.107

PRODUCT_NAME = TaskFollowUp
